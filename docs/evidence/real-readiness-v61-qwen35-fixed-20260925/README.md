# 実ローカルLLM受入証拠 — v61

- 固定系列: `real-readiness-v61-qwen35-fixed-20260925`
- 固定コード: `12393db`
- モデル: Ollama `agentteam-qwen35-9b-16k`（同時実行1件、Claude/クラウドへの自動切替なし）
- 実行対象: リポジトリの実資料、実API/SQLite、実サービスを使う代表文書ワークフロー
- 実行日時: 2026-09-25（JST、個別のUTC時刻は `attempts.json` と実行領域に保存）
- 目標: 同じ構成の実ローカルLLM実行10回
- 状態: 10回すべてで機械的契約・配信契約を通過。9回はReviewer提出まで到達し、提出9回すべてが4基準pass。1回は壁時計上限でReviewer提出前に中断。シリーズ全体の意味審査は未完了。`production_ready=false`。L3判定なし。

## 結果

| 指標 | 結果 |
| --- | ---: |
| 実行回数 | 10 |
| 機械的契約通過 | 10/10 |
| 配信契約通過 | 10/10 |
| `readiness.json`生成 | 10/10（公開アーティファクトの生バイトを保存） |
| 実行状態 | `completed` 9、`interrupted` 1 |
| Reviewer提出 | 9/10 |
| Reviewerの4基準すべてpass | 9/9提出（シリーズ全体では9/10） |
| シリーズ全体の意味審査 | `pending` |
| 本番導入判定 | `false` |
| モデル呼出し / ツール呼出し | 206 / 156 |
| 合計実行時間 | 12,717.61秒 |
| 受入済み | 0/10 |

要約を資料の英語履歴から固定したことで、v60で再現した要約欄の禁止語ループは発生しなかった。9回のReviewer提出は、現在のアーティファクトに対する独立した `json_schema` 検査を経ており、4基準すべてがpassだった。1回は同じ機械的・配信契約を通過した後、壁時計上限で提出前に中断したため未評価として保持する。

これは出力契約とレビュー手順の安定性を示すローカル観測であり、業務品質、出典の意味的一致、見逃し・誤検出率、顧客受入、L2/L3、本番導入可能性を意味しない。意味審査が未完了で、シリーズ受入は0/10のままとする。

## 変更の評価

v61は `12393db` の要約契約修正後に実行した新しい固定系列である。生成物を直接書き換えて成功扱いにはしていない。下記のドラフトは各試行で生成された `readiness.json` の生バイトをそのまま保存し、SHA-256を検証してからコピーした。`artifacts.json` はサービスが記録したアーティファクト一覧である。旧合成比較や顧客環境の証拠には使用しない。 `independent-check.json` は8領域の順序・引用・要約固定文・SHA-256と9件のReviewer判定を別スクリプトで再確認した記録である。

## 保持場所

完全なSQLite、イベント、設定、全試行の作業領域はローカルの`/Users/horioshuuhei/.cache/agentteam-bench/real-readiness-v61-qwen35-fixed-20260925`に保持している。公開証拠には秘密情報を含めないよう、状態、試行メタデータ、アーティファクト一覧、生成JSONの生バイトだけをコピーした。
