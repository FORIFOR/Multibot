# 実ローカルLLM受入証拠 — v58

- 固定系列: `real-readiness-v58-qwen35-fixed-20260925`
- 固定コード: `8aca9aa`
- モデル: Ollama `agentteam-qwen35-9b-16k`（同時実行1件、Claude/クラウドへの自動切替なし）
- 実行対象: リポジトリの実資料、実API/SQLite、実サービスを使う代表文書ワークフロー
- 実行日時: 2026-09-25（JST、個別のUTC時刻は`attempts.json`と実行領域に保存）
- 目標: 同じ構成の実ローカルLLM実行10回
- 状態: 10回の機械的・配信契約を完了。Reviewer提出は10回、うち6回が4基準すべてpass、4回は`document_contract`と`document_accuracy`がfail。シリーズ全体の意味審査は未完了。`production_ready=false`。L3判定なし。

## 結果

| 指標 | 結果 |
| --- | ---: |
| 実行回数 | 10 |
| 機械的契約通過 | 10/10 |
| 配信契約通過 | 10/10 |
| `readiness.json`生成 | 10/10（公開アーティファクトの生バイトを保存） |
| 実行状態 | `completed` 6、`partial` 4 |
| Reviewer提出 | 10/10 |
| Reviewerの4基準すべてpass | 6/10 |
| Reviewer判定内訳 | pass 32、fail 8 |
| シリーズ全体の意味審査 | `pending` |
| 本番導入判定 | `false` |
| モデル呼出し / ツール呼出し | 190 / 144 |
| 合計実行時間 | 7,819.21秒 |

4回のReviewer failは、Reviewerが独立した`json_schema`チェックを実行せず、英語の`evidence_quote`を日本語訳と誤認したことに起因する。機械的契約では各生成物の生バイトを検証しており、これらのレビュー結果を成功へ書き換えていない。v58の結果を受入済みとは扱わない。

10回すべてで機械的契約と配信契約を通過したこと、また一部のReviewer提出がpassだったことは、業務品質、出典の意味的一致、顧客受入、L2/L3、本番導入可能性を意味しない。Reviewer fail 4回と、意味審査が未完了のためシリーズ受入は0/10のままとする。

## 変更の評価

v58は、v57の固定系列の後に、同じ実資料・モデル・設定で実行した新しい系列である。生成後に成果物を編集して成功扱いにしていない。下記のドラフトは各試行で生成された`readiness.json`の生バイトをそのまま保存したものであり、`artifacts.json`はサービスが記録したアーティファクト一覧である。各試行の最新`readiness.json` revisionを保持し、SHA-256を検証してからコピーした。旧合成比較や顧客環境の証拠には使用しない。

## 保持場所

完全なSQLite、イベント、設定、全試行の作業領域はローカルの`/Users/horioshuuhei/.cache/agentteam-bench/real-readiness-v58-qwen35-fixed-20260925`に保持している。公開証拠には秘密情報を含めないよう、リポジトリにはステータス、試行メタデータ、アーティファクト一覧、生成JSONの生バイトだけをコピーした。
