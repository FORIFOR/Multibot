# 実ローカルLLM受入証拠 — v55

- 固定系列: `real-readiness-v55-qwen35-fixed-20260924`
- 固定コード: `b39ba74`
- モデル: Ollama `agentteam-qwen35-9b-16k`（同時実行1件、Claude/クラウドへの自動切替なし）
- 実行対象: リポジトリの実資料、実API/SQLite、実サービスを使う代表文書ワークフロー
- 実行日時: 2026-09-24（JST、個別のUTC時刻は`attempts.json`と実行領域に保存）
- 目標: 同じ構成の実ローカルLLM実行10回
- 状態: 10回の機械的試験を完了。機械的・配信契約は全回通過したが、Reviewer提出は2回に留まり、シリーズ全体の意味審査は未完了。`production_ready=false`。L3判定なし。

## 結果

| 指標 | 結果 |
| --- | ---: |
| 実行回数 | 10 |
| 機械的契約通過 | 10/10 |
| 配信契約通過 | 10/10 |
| `readiness.json`生成 | 10/10（公開アーティファクトの生バイトを保存） |
| 実行状態 | `partial` 9、`interrupted` 1 |
| Reviewer提出 | 2 |
| Reviewer未完了 | 8（時間上限またはモデル呼出し上限） |
| シリーズ全体の意味審査 | `pending` |
| 本番導入判定 | `false` |
| モデル呼出し / ツール呼出し | 196 / 124 |
| 合計実行時間 | 8,598.28秒 |

10回すべてで機械的契約と配信契約を通過し、`source_file`と`evidence_quote`を含む生成JSONを保持した。一方、Reviewer提出は2回だけで、残り8回は独立レビューの完了まで到達しなかった。したがって、機械的通過を業務品質、顧客受入、L2/L3、本番導入可能性へ読み替えていない。シリーズ受入は0/10のままである。

## 変更の評価

v55は固定コード`b39ba74`（v54の証拠を記録した後の新しい固定系列）で、v54と同じ実資料・モデル・設定を使って実行した。v54では10回中8回がReviewer提出まで到達したが、v55では2回に戻った。これは、固定系列ごとに完了率が安定していないことを示す観測であり、業務品質の再現性を示すものではない。出典・要約の意味的正確性、レビュー見逃し・誤検出、レイテンシと受入閾値は未確定である。

生成後に成果物を編集して成功扱いにしていない。下記のドラフトは各試行で生成された`readiness.json`の生バイトをそのまま保存したものであり、`artifacts.json`はサービスが記録したアーティファクト一覧である。Revisionが複数ある試行はすべて保持した。

## 保持場所

完全なSQLite、イベント、設定、全試行の作業領域はローカルの`/Users/horioshuuhei/.cache/agentteam-bench/real-readiness-v55-qwen35-fixed-20260924`に保持している。公開証拠には秘密情報を含めないよう、リポジトリにはステータス、試行メタデータ、アーティファクト一覧、生成JSONの生バイトだけをコピーした。旧合成比較や顧客環境の証拠には使用しない。
