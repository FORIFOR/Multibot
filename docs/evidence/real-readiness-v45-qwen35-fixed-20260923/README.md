# 実ローカルLLM受入証拠 — v45

- 固定系列: `real-readiness-v45-qwen35-fixed-20260923`
- 固定コード: `cef86aa`
- モデル: Ollama `agentteam-qwen35-9b-16k`（同時実行なし、Claude/クラウドフォールバックなし）
- 目標: 同じ代表文書ワークフロー10回
- 状態: 3回目の実行中、同じ形式未達が2回連続したため停止。1・2回目は1,200秒超で中断し、3回目は停止時点の状態を保存した。Reviewer提出0、受入0。L3判定なし。

## 結果

| 回 | 状態 | 実行時間 | 主な観測 |
|---:|---|---:|---|
| 1 | 中断 | 1,200.82秒 | `readiness.json` が無い、または有効なJSONでない |
| 2 | 中断 | 1,200.55秒 | `readiness.json` が無い、または有効なJSONでない |
| 3 | 停止 | — | 2回の同一失敗を保存して停止。結果オブジェクトは未確定 |

完全な試行記録は [`attempts.json`](attempts.json) と [`status.json`](status.json) に保存した。v45では有効な配信成果物は確認されず、生成途中の成果物を成功扱いのために編集していない。

## 変更の評価

v43のワークフロー指定漏れとv44のJSON契約バリデーション不整合を修正したコードで、実ローカルLLMの同一業務を再検証した。入力受付は通過したが、Builderが要求された `readiness.json` を公開できない状態が繰り返されたため、残りの自動反復を開始せず停止した。Reviewer提出と受入は0件であり、業務品質や本番導入可能性を示すものではない。

## 保持場所

完全なSQLite、イベント、設定、全試行の作業領域はローカルの `/Users/horioshuuhei/.cache/agentteam-bench/real-readiness-v45-qwen35-fixed-20260923` に保持している。合成比較や顧客環境の証拠には使用しない。
