# 実ローカルLLM受入証拠 — v56

- 固定系列: `real-readiness-v56-qwen35-fixed-20260924`
- 固定コード: `629f7a3`
- モデル: Ollama `agentteam-qwen35-9b-16k`（同時実行1件、Claude/クラウドへの自動切替なし）
- 実行対象: リポジトリの実資料、実API/SQLite、実サービスを使う代表文書ワークフロー
- 実行日時: 2026-09-24（JST、個別のUTC時刻は`attempts.json`と実行領域に保存）
- 目標: 同じ構成の実ローカルLLM実行10回
- 状態: 10回の機械的試験を完了。機械的・配信契約は全回通過し、Reviewer提出も全10回に到達したが、4回はReviewerのいずれかの基準がfail。シリーズ全体の意味審査は未完了。`production_ready=false`。L3判定なし。

## 結果

| 指標 | 結果 |
| --- | ---: |
| 実行回数 | 10 |
| 機械的契約通過 | 10/10 |
| 配信契約通過 | 10/10 |
| `readiness.json`生成 | 10/10（公開アーティファクトの生バイトを保存） |
| 実行状態 | `completed` 6、`partial` 1、`interrupted` 3 |
| Reviewer提出 | 10/10 |
| Reviewerの4基準すべてpass | 6/10 |
| Reviewer判定内訳 | pass 32、fail 8（4回にfailあり） |
| シリーズ全体の意味審査 | `pending` |
| 本番導入判定 | `false` |
| モデル呼出し / ツール呼出し | 230 / 193 |
| 合計実行時間 | 8,777.02秒 |

Reviewer向けの`run_check`呼び出しを「公開ID・revisionをトップレベルに置き、依頼者JSON契約では`args`を省略する」形に明示した固定コードで再実行した。その結果、v55で2/10だったReviewer提出は10/10へ改善した。一方、4回の審査では`document_contract`、`document_request`または`document_accuracy`のfailが残り、意味品質の反復受入は成立していない。

10回すべてで機械的契約と配信契約を通過したことは、業務品質、出典の意味的一致、顧客受入、L2/L3、本番導入可能性を意味しない。Reviewerのpass/failは各試行の独立判定であり、シリーズ全体の受入判定ではない。シリーズ受入は0/10のままである。

## 変更の評価

v56は固定コード`629f7a3`（v55の証拠を記録した後の新しい固定系列）で、v55と同じ実資料・モデル・設定を使って実行した。v55ではReviewer提出が2/10だったため、`run_check`の正しい引数形をruntimeのToolSpec、workerのレビュー指示、Reviewerプロンプトに明示した。v56では10/10が提出まで到達したが、4/10に意味・要求充足・契約のfailが残った。したがって、ツール呼び出しの再現性は改善したものの、生成物の意味品質は未達である。

生成後に成果物を編集して成功扱いにしていない。下記のドラフトは各試行で生成された`readiness.json`の生バイトをそのまま保存したものであり、`artifacts.json`はサービスが記録したアーティファクト一覧である。Revisionが複数ある試行は、最新の`readiness.json` revisionを保持した。旧合成比較や顧客環境の証拠には使用しない。

## 保持場所

完全なSQLite、イベント、設定、全試行の作業領域はローカルの`/Users/horioshuuhei/.cache/agentteam-bench/real-readiness-v56-qwen35-fixed-20260924`に保持している。公開証拠には秘密情報を含めないよう、リポジトリにはステータス、試行メタデータ、アーティファクト一覧、生成JSONの生バイトだけをコピーした。
