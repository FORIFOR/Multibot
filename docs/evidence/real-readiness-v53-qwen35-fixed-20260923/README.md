# 実ローカルLLM受入証拠 — v53

- 固定系列: `real-readiness-v53-qwen35-fixed-20260923`
- 固定コード: `c5bbec1`
- モデル: Ollama `agentteam-qwen35-9b-16k`（同時実行1件、Claude/クラウドへの自動切替なし）
- 実行対象: リポジトリの実資料、実API/SQLite、実サービスを使う代表文書ワークフロー
- 実行日時: 2026-09-23（JST、個別のUTC時刻は`attempts.json`と実行領域に保存）
- 目標: 同じ構成の実ローカルLLM実行10回
- 状態: 10回の機械的試験を完了。全10回で機械的契約と配信契約は通過したが、意味審査・契約受入が完了せず、受入0・`production_ready=false`。L3判定なし。

## 結果

| 指標 | 結果 |
| --- | ---: |
| 実行回数 | 10 |
| 機械的契約通過 | 10/10 |
| 配信契約通過 | 10/10 |
| `readiness.json`生成 | 10/10（公開アーティファクトの生バイトを保存） |
| 実行状態 | `partial` 4、`interrupted` 4、`failed` 2 |
| Reviewer提出 | 2 |
| Reviewer提出なし | 8（時間上限またはモデル呼出し上限） |
| 意味審査 | `pending` |
| 受入済み | 0 |
| 本番導入判定 | `false` |
| モデル呼出し / ツール呼出し | 301 / 247 |
| 合計実行時間 | 10,228.78秒 |

全10回で同一の`readiness.json`公開形状が機械的に検査され、`source_file`と`evidence_quote`を含む8領域が保持された。一方、Reviewer提出があった2回も`document_contract`を未検証として残し、残り8回は独立Reviewerの提出まで到達しなかった。したがって、機械的通過を業務品質、顧客受入、L2/L3、本番導入可能性へ読み替えていない。

## 変更の評価

v53は固定コード`c5bbec1`で実行し、`workspace_write_json`の修復テンプレートに現行Schemaの共有定数、全必須行フィールド、ソース由来の日本語例を提示した系列である。v52まで反復した「有効なJSONを公開できない」失敗は、今回の10回すべてで機械的には解消された。ただし、Reviewerが意味審査を完了した10回の受入結果はなく、2回の提出も`document_contract`を未検証として保持した。出典・要約の意味的正確性、レビュー見逃し・誤検出、レイテンシと受入閾値、同一条件での再現性は未確定である。

生成後に成果物を編集して成功扱いにしていない。下記のドラフトは各試行で生成された`readiness.json`の生バイトをそのまま保存したものであり、`artifacts.json`はサービスが記録したアーティファクト一覧である。

## 保持場所

完全なSQLite、イベント、設定、全試行の作業領域はローカルの`/Users/horioshuuhei/.cache/agentteam-bench/real-readiness-v53-qwen35-fixed-20260923`に保持している。公開証拠には秘密情報を含めないよう、リポジトリにはステータス、試行メタデータ、アーティファクト一覧、生成JSONの生バイトだけをコピーした。旧合成比較や顧客環境の証拠には使用しない。
