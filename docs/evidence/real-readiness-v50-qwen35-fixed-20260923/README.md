# 実ローカルLLM受入証拠 — v50

- 固定系列: `real-readiness-v50-qwen35-fixed-20260923`
- 固定コード: `16bdb0e`
- モデル: Ollama `agentteam-qwen35-9b-16k`（同時実行なし、Claude/クラウドフォールバックなし）
- 目標: 同じ代表文書ワークフロー10回
- 状態: 4回目を開始したが、最初の3回が同じ失敗だったため停止。Reviewer提出0、受入0。L3判定なし。

## 結果

| 回 | 状態 | 実行時間 | 主な観測 |
|---:|---|---:|---|
| 1 | 失敗 | 1,033.54秒 | `readiness.json` が無い、または有効なJSONでない。Builderがツールを呼ばずに終了し、Masterも独立レビューを開始できなかった |
| 2 | 失敗 | 958.43秒 | 同じく有効なJSONなし。Builderの反復が`max_tokens`で終了し、ツール呼出し0 |
| 3 | 失敗 | 1,010.18秒 | 同じく有効なJSONなし。Builderの反復が`max_tokens`で終了し、ツール呼出し0 |
| 4 | 停止 | — | 3回連続の同一失敗を保存して停止。結果オブジェクトは未確定 |

完全な試行記録は [`attempts.json`](attempts.json) と [`status.json`](status.json) に保存した。各試行のイベント・アーティファクト記録と、作業領域に残った生成バイト列もそのまま保存した。生成物を成功扱いのために編集していない。

## 変更の評価

v50は、`workspace_write_json` の動的な依頼スキーマに `source_file`・完全一致引用の必須説明を追加した `16bdb0e` で実行した。実行ではBuilderがJSONツールを一度も呼ばず、最大出力で反復したため、スキーマ修正の効果を判定できる有効成果物は得られなかった。これは実装上のツール呼出し誘導と出力上限の課題を示すが、業務品質・顧客受入・L3の証拠ではない。

4回目は停止後の実行記録が未確定であり、成功件数へ加算しない。旧合成比較とは系列を分離し、結果を混ぜていない。

## 保持場所

完全なSQLite、イベント、設定、全試行の作業領域はローカルの `/Users/horioshuuhei/.cache/agentteam-bench/real-readiness-v50-qwen35-fixed-20260923` に保持している。合成比較や顧客環境の証拠には使用しない。
