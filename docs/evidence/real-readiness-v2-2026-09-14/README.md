# Actual-source readiness workflow v2 — paused after two failures

This series used the fixed `c69c27ddc15eb489be8f675e0804a3e9c9b2cbba` checkout, the real local `agentteam-qwen35-9b-16k` model (Ollama 0.33.3), the actual repository `PRODUCTION_PLAN.md` and operations verification record, and the secured in-process API/durable queue. It did not use Claude, a cloud fallback, synthetic business records or a replacement response. The source, model digest, configuration fingerprint, probe, events and every artifact revision are retained here.

The series was intentionally stopped after two attempts for inspection. Attempt 1 reached the 900-second wall limit while the reviewer was still working. Attempt 2 produced three revisions and a reviewer finding, but still copied the English `Remaining acceptance work` strings into `remaining`; the runtime rejected `finish_task` with the requester-owned schema and the wall limit then interrupted the run. It was not counted as a successful repetition. `attempts.json` records both statuses and mechanical grades; the stopped process did not rewrite any artifact as success.

The reviewer correctly identified the distinction in its finding: `evidence_quote` must remain the exact English source quote, while `remaining` must be a Japanese summary. The model's correction changed some `implemented` text but failed to translate all `remaining` fields and then spent time debating the schema. This is a real model/workflow usability failure. The mandatory gate did its job: no false `completed` status was emitted. The series contains no ten-run business-quality result.

The next fixed series adds a concrete rejection message and schema descriptions that state this distinction, while preserving the same source-derived quote constraints. These are guidance improvements, not a claim that Japanese meaning or terminology is automatically correct. Technical terms such as `age`, `readiness`, `actor-scoped`, `WORM` and `SSE` must retain their meaning; the model output still requires semantic inspection.

The two attempts consumed roughly 901 seconds each and 23/22 model calls respectively. Local-model wall time varies materially by prompt and output; these measurements are recorded for planning only. They are not latency or availability guarantees. v2 remains a failed/paused evidence series and does not grant L1, L2 or L3.
