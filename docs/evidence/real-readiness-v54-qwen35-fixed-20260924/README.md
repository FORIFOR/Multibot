# 実ローカルLLM受入証拠 — v54

- 固定系列: `real-readiness-v54-qwen35-fixed-20260924`
- 固定コード: `27b5100`
- モデル: Ollama `agentteam-qwen35-9b-16k`（digest `c1b119d707b9…`、同時実行1件、Claude/クラウドへの自動切替なし）
- 実行対象: リポジトリの実資料、実API/SQLite、実サービスを使う代表文書ワークフロー
- 実行日時: 2026-09-24（JST、個別のUTC時刻は`attempts.json`と実行領域に保存）
- 目標: 同じ構成の実ローカルLLM実行10回
- 状態: 10回の機械的試験を完了。機械的・配信契約は全回通過した。独立Reviewerは6回で全基準を通過、1回は提出後に中断、3回は完了操作に到達しなかった。シリーズ全体の意味審査は未完了で、`production_ready=false`。L3判定なし。

## 結果

| 指標 | 結果 |
| --- | ---: |
| 実行回数 | 10 |
| 機械的契約通過 | 10/10 |
| 配信契約通過 | 10/10 |
| `readiness.json`生成 | 10/10（公開アーティファクトの生バイトを保存） |
| 実行状態 | `completed` 6、`partial` 3、`interrupted` 1 |
| Reviewer提出 | 8 |
| Reviewerタスク受入 | 6（4基準すべてpass） |
| Reviewer提出後の中断 | 1 |
| Reviewer未完了 | 3（`finish_task`未到達） |
| シリーズ全体の意味審査 | `pending` |
| 本番導入判定 | `false` |
| モデル呼出し / ツール呼出し | 235 / 181 |
| 合計実行時間 | 8,461.66秒 |

6回の独立Reviewer受入では、JSON妥当性、`PRODUCTION_PLAN.md`との照合、契約、言語、依頼内容の各基準を確認し、`production_ready=false`を正しい状態として扱った。ただし10回すべての意味審査が完了したわけではなく、残り4回を含むシリーズ受入は0/10である。機械的通過や一部Reviewer受入を、業務品質、顧客受入、L2/L3、本番導入可能性へ読み替えていない。

## 変更の評価

v54は固定コード`27b5100`（v53の証拠を記録した後の新しい固定系列）で、v53と同じ実資料・モデル・設定を使って実行した。v53で確認した「機械的契約は通るがReviewer提出が時間上限に達する」問題に対し、今回の10回では8回がReviewer提出まで到達し、6回は全基準をpassした。残る中断・未完了を含めた再現性、出典と要約の意味的正確性、レビュー見逃し・誤検出、レイテンシと受入閾値は未確定である。

生成後に成果物を編集して成功扱いにしていない。下記のドラフトは各試行で生成された`readiness.json`の生バイトをそのまま保存したものであり、`artifacts.json`はサービスが記録したアーティファクト一覧である。Revisionが複数ある試行はすべて保持した。

## 保持場所

完全なSQLite、イベント、設定、全試行の作業領域はローカルの`/Users/horioshuuhei/.cache/agentteam-bench/real-readiness-v54-qwen35-fixed-20260924`に保持している。公開証拠には秘密情報を含めないよう、リポジトリにはステータス、試行メタデータ、アーティファクト一覧、生成JSONの生バイトだけをコピーした。旧合成比較や顧客環境の証拠には使用しない。
