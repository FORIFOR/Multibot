# 実ローカルLLM受入証拠 — v60

- 固定系列: `real-readiness-v60-qwen35-fixed-20260925`
- 固定コード: `69c37a7`
- モデル: Ollama `agentteam-qwen35-9b-16k`（同時実行1件、Claude/クラウドへの自動切替なし）
- 実行対象: リポジトリの実資料、実API/SQLite、実サービスを使う代表文書ワークフロー
- 実行日時: 2026-09-25（JST、個別のUTC時刻は保持領域と `attempts.json` に保存）
- 目標: 同じ構成の実ローカルLLM実行10回
- 状態: 同じ実行構成で3回連続してBuilderが `readiness.json` を生成できず、4回目の開始後に意図的に停止。公開アーティファクト0、Reviewer提出0、受入0/10。`production_ready=false`。L3判定なし。

## 結果

| 指標 | 結果 |
| --- | ---: |
| 目標実行回数 | 10 |
| 保持された完了試行 | 3 |
| 同一原因による中断 | 3/3 |
| `readiness.json`公開 | 0/3 |
| Reviewer提出 | 0/3 |
| 4回目 | 開始後に停止（証拠領域作成前） |
| 受入済み | 0/10 |
| 本番導入判定 | `false` |

3回とも、v58/v59の履歴を含む現行 `PRODUCTION_PLAN.md` をモデルが要約欄へ英語・混在語付きでコピーし、配信契約の禁止語検査に失敗した後、同じ内容を再送して1,200秒の壁時計上限に達した。4回目も同じ再試行に入ったため、同一原因の再現を保持したうえで系列を停止した。生成物を直接書き換えて成功扱いにはしていない。

## 実装上の修正

原因は、資料の履歴を含むBusiness quality行が要約へ漏れ、モデルが禁止語を修復できずループする入力・契約の組合せだった。次の固定系列では、要約欄を事実に基づく安全な一文へ明示し、配信契約でも同じ文だけを許可する。

`12393db` でこの修正を実装し、v60の3試行を終えた後にコミット・pushした。これはv60の結果を成功へ変更するものではなく、新しい固定系列を開始する根拠である。

## 保持場所

完全なSQLite、イベント、設定、全試行の作業領域はローカルの`/Users/horioshuuhei/.cache/agentteam-bench/real-readiness-v60-qwen35-fixed-20260925`に保持している。公開証拠には秘密情報を含めないよう、状態、試行メタデータ、アーティファクト一覧、実行メタデータだけをコピーした。v60には有効な成果物がない。
