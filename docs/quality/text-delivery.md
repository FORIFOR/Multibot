# Text delivery contract — continuing failed outcome checks

Base `aad83ddf682606fc024b29b5855273472d345db8` plus existing working edits. User explicitly requested continuation until PASS, superseding the earlier three-round stopping guideline. Original requirements remain unchanged. Historical reports remain historical; newer evidence must not overwrite failed artifacts.

## Frozen task
Actual attached integration.md → Japanese guide.md, three requested headings, 400–700 Unicode code points including Markdown/whitespace, accurate HTTP 202/selected ZIP/ambiguous retry conditions with source section references. A fresh request must yield a correctly checked file within the existing 480-second execution allowance, without explicit resume. Saving must reproduce its exact selected bytes. No third-party model or web tool, mock provider, fake event, or manual replacement output.

## Implementation
- Extend existing requester-owned `delivery_requirements` with `input_format: "text"`; default `"json"` retains previous parsing. JSON Schema draft 2020-12 string constraints apply to the strict UTF-8 decoded file, with no trimming or normalization. Old default JSON serialization remains identical, including persisted request receipt hashes.
- Same isolated process/time/input limits as existing JSON validation. `run_check(kind=json_schema, artifact_id, revision)` with omitted schema uses both requester schema and format. Explicit schemas retain JSON default unless args explicitly sets input_format. Finish/finalization enforce the persisted requirement independently of model claims.
- Home offers optional output filename/minimum/maximum character fields, persisted with the draft. The UI creates the contract; enforcement lives in runtime. Blank fields preserve existing behavior. Byte/character/code-point and correctness limitations are explained.
- Token-limit recovery rebuilds the actual task message, source context, human directions and review findings; it retains the no-tool retry counter. Remove unrelated readiness.json-specific file/content/translation instructions from the shared builder and repair paths. Validation verdicts and source-specific requester schemas remain intact.

## Evidence to collect
Relevant real-document/SQLite regression tests; build/typecheck; real browser input/draft → model-generated file/check events → selected ZIP SHA-256; compare content to its stored input; independent review. Failure logs retained. Native N/A. Human evaluation, physical-device and OS IME remain unperformed until those environments/people actually participate.

Round 1 uses the same installed local model `agentteam-qwen35-9b-16k`, team mode with independent reviewer and 480-second limit. The effective per-response output cap was3000, confirmed retrospectively from the stored config_snapshot. A direct YAML edit intended to set1800 was overwritten on startup by the latest SQLite configuration revision. This correction also applies to round2; the earlier1800 claim was inaccurate. No global configuration was changed.

## Round 1 result
FAIL: `run_1a0b9201f5dd63de296` interrupted at 480.53s, 17 model attempts, no published artifact. The final unpublished draft remained1219 code points. Preserve `artifacts/product-quality/text-delivery-1/{run.json,events.json,outcome.json,unpublished-guide.md}` and browser exit1. Model-generated checks printed a length without asserting its bound and treated tool exit0 as passing. Runtime final checks were not reached because the model kept editing/checking without publication.

Round 2 adds an automatic call to the same contract check immediately after workspace_write. It keeps the draft even if invalid, identifies it as not yet published, and returns concise actual length/max/min failures. Checks on workspace paths now resolve the persisted contract as published-artifact checks do. `delivery.checked` is included in version-detail results so required-condition failures are visible and bound to exact bytes. This does not change the400–700/480s criteria.

Independent source review found no new release-blocking issue in this scope after the existing-message recovery fix. Regression evidence includes the old-revision failure API check. The frozen round2 source hashes are in `artifacts/product-quality/text-delivery-2/source-manifest.json`. Subsequent verification test additions do not modify the running implementation.

## Genuine browser zoom
The previous real-browser200% item is now PASS for tested Chrome/flows: [browser-native-zoom.md](browser-native-zoom.md). Independent temporary Chrome profile changed its actual Page zoom setting to200%; innerWidth1440→720 and devicePixelRatio1→2 while document CSS zoom remains1. Keyboard download and saved-byte SHA-256 matched. An initially suspected overlap was disproved by boundary measurements; the experimental layout change was reverted before delivery. This does not verify human usability or OS Japanese IME. `osascript -e 'tell application "System Events" to get UI elements enabled'` returnedfalse, exit0: OS input control is unavailable, and no accessibility permission setting was changed.

Official transport reference checked2026-09-19: [Ollama OpenAI compatibility](https://docs.ollama.com/api/openai-compatibility) lists reasoning_effort=none support. Existing driver uses this field for disabled thinking. Do not infer that thinking caused latency without runtime evidence.

## Round 2 result
FAIL: `run_1a0b92857064896e6ba` interrupted at480.37s with no published artifact. Automatic contract feedback correctly rejected764 and707 code points; the model then rewrote the same707-character draft instead of satisfying the bound. Logs and unmodified unpublished draft remain under `artifacts/product-quality/text-delivery-2`. The failure was not hidden by successful shell exit statuses. The next attempt adds an explicit required removal count to length feedback and tests the already installed `agentteam-qwen25-7b-16k` with the same task, independent reviewer,1800-token response cap and480-second deadline. New model transport capabilities must be probed before starting. This is a different measured environment, not proof that9B passed or that success is universal.

## Round 3 environment correction
Use PUT /api/limits with expected_revision to persist the model/output-cap change. GET /api/config confirms revision4,7B,1800 and capability_detail.model_requested/model_reported both7B with tool_calling/json_schema passed. The probe HTTP client timed out before completion; its server-side result was subsequently confirmed without resubmission. Evidence: `text-delivery-3/effective-config.json`, `config-update.json`. A separate existing production_workflow process is using the same Ollama endpoint; it was left untouched. Timings include this resource contention. Source manifest was captured before the new browser request.

## Model changes cannot reuse stale capability evidence
Independent audit found that defaults/individual-agent model changes reused another model's passed connection probe. Core precheck now compares every enabled agent with `model_requested` before the connection-level deduplication. Missing metadata requires a probe; separate connection IDs are required for multiple models. CLI and real benchmark scripts record and recheck the requested model. Actual v18 Ollama probe evidence, existing 9B/7B profiles, SQLite and HTTP APIs verify original model acceptance, changed defaults/individual-agent rejection, reversed agent order and absent metadata. All runs use start:false and model_calls remains0. Related20 tests PASS, command/log: `outcome-capability-full-regression.log` (exit0). This change is not loaded into the running round3 process.

## Latest repository changes and plan checks
HEAD advanced to `e3448595e13aceb1b4ad5a6fbd92154e4de1a9ac` during this work. Its v27 evidence, production-workflow source requirement and tests were retained. Nine recorded-output regression tests pass (`outcome-latest-user-regressions.log`, exit0). Independent review found one more probe writer in reviewer_catch_rate.py; it now retains requested-model evidence too.

Round3's model planned headings as separate files, omitting guide.md. Before another attempt, validate_plan now rejects missing requester delivery paths, with a regression based on an actual recorded plan. Related21 tests pass (`outcome-plan-path-regression.log`, exit0). Worker instructions now explicitly advise drafting within the range with room below the maximum and using workspace_write's actual validation feedback. No range or deadline was changed. Optional-condition UI independent checks passed: [delivery-condition-independent.md](delivery-condition-independent.md).

## Round 3 result and opt-in document workflow
FAIL: run_1a0b93630b7c6e20c07 reached480.095s,5 model attempts,6 tool calls,0 artifacts. Two planning calls consumed about206.6s and produced the wrong file plan. Browser timeout exit1 and complete run/events/outcome are preserved under text-delivery-3. Resource contention remains present; this is not isolated latency evidence.

Next measurement explicitly opts into a document workflow (same source-to-file goal,400–700 bounds and480s deadline). Core compiles the fixed builder→independent reviewer sequence from the actual request and declared path; no generated artifact, verdict or event is substituted. Generation and review still call the real local model. This removes unnecessary planning inference for this narrow, explicitly selected task. Inputs and missing roles fail closed; default serialization remains unchanged. Milestone/exception replanning is disabled for this workflow; bounded review corrections remain. Evidence before the next model trial:22 real-document/SQLite tests passed (`outcome-document-regression.log`); build/typecheck passed (`outcome-document-build.log`).

## Independent follow-up fixes
An independent code review found that a stale review reference could accept a newer artifact. A shared latest-task-reference check now guards submission and scheduler acceptance; changes between the two leave both tasks partial. Real v18 immutable artifacts and recorded review exercise old/foreign rejection and post-submission changes.23 tests PASS (`outcome-exact-review-regression.log`, exit0). Independent re-review found the reported hole fixed. This backend change is not loaded into the running round4 process.

Home's explicit document workflow checkbox passed independent390px, actual200% browser zoom, keyboard, draft restore/discard checks with no POST (`document-workflow-independent.md`). Lint exit0 retains existing warnings (`outcome-document-lint.log`). Shared config precheck still requires a valid configured team (including master); document workflow skips its planning call, not the normal team configuration checks.

## Round 4 result and regression wording correction
FAIL: document-workflow run_1a0b9418a6a18cdede3 interrupted at480.016s,7 model attempts,6 tool calls,0 artifacts. Drafts860→826→770 then repeated the same770 bytes. The recorded draft also incorrectly says that omitting selected ZIP selection returns409; this still violates source fidelity. The unedited draft, SHA-256, full run/events and browser log remain in text-delivery-4. Nothing was adopted as a correct result. The first model response took177.255s in the shared environment; no isolated latency conclusion is claimed. Repeated identical workspace writes now explicitly report UNCHANGED rather than implying a repair occurred.

The expanded36-test run initially failed one old string assertion (`outcome-final-core-regression.log`, exit1). It expected readiness-specific evidence_quote and a Japanese repair phrase that had been removed from generic runtime prompts. Actual validation errors concern remaining fields; evidence_quote equality is still independently enforced by the requester JSON Schema const. Independent audit approved replacing those incidental phrase assertions with actual remaining violations and the generic safety requirements: preserve source facts, do not weaken schema, publish/recheck a new revision. Rejection, unfinished context, auto-finish refusal, partial final state, FAIL evidence and unchanged original bytes remain asserted. No delivery threshold, schema, verdict or output expectation was relaxed.

## Current checkpoint
- PASS:36 relevant real-document/SQLite/security/durable-execution tests, `outcome-final-core-verified.log`, exit0; build/typecheck and lint exit0 (warnings retained). Independent UI and code checks linked above.
- FAIL: fresh source-to-guide first success/source accuracy. Four attempts are recorded; latest has no published artifact and an invalid770-character draft. No resume/manual replacement is counted as first success.
- BLOCKED: uncontended timing and same-task local CLI comparison while the existing ten-run benchmark shares Ollama; actual user evaluation, OS Japanese IME and touch hardware require human/environment participation. The independent benchmark was not stopped.
- NOT_APPLICABLE: native app testing; supported UI here is Web.

Latest local service was restarted on127.0.0.1:8796 with the reviewed fixes after the trial ended. GET /api/config has no problems and matches the probed9B model. No new model task was started on restart. Exact current source hashes, config and environment are in `artifacts/product-quality/final-continuation/`; HEAD is e3448595e13aceb1b4ad5a6fbd92154e4de1a9ac plus the recorded working changes. Dist/bundled UI byte equality was checked. No push, publish, deployment, cloud-model call or third-party data submission occurred.
